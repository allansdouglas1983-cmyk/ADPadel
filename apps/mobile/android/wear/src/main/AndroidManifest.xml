<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.type.watch" />
    <!-- Health Connect for HR / calories during a match. -->
    <uses-permission android:name="android.permission.health.READ_HEART_RATE" />

    <application
        android:label="Marque"
        android:theme="@android:style/Theme.DeviceDefault">

        <!-- Standalone: the watch app works without the phone app installed. -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Keeps the active match alive across navigation. -->
        <service
            android:name=".MatchForegroundService"
            android:foregroundServiceType="specialUse"
            android:exported="false" />
    </application>
</manifest>
